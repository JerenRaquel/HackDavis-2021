using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SavingsAccountButtonController : MonoBehaviour
{
    // Start is called before the first frame update
    void Deposit()
    {
        Debug.log("Depositing into Savings Account");
        if (Checkings.GetValue() < text box input value) {
           
        }
    }

    // Update is called once per frame
    void Withdraw()
    {

    }
}
